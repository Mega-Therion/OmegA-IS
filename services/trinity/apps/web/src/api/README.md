# Public API Routes (apps/web)

These are public‑safe endpoints intended for Vercel exposure.

- `POST /api/intent` → write intent record (no execution)
- `GET /api/status`
- `GET /api/runs/:id`

Implement using your web framework of choice (Vite + server, or Next API routes).
